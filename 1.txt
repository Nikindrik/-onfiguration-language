\ Это комментарий
name := @"Имя пользователя"
age := 30
is_active := true

\ Пример массива
colors := { @"red". @"green". @"blue" }

\ Пример словаря
user_info := ([
  name : @"Nick",
  age : 10,
  active : true
])

\ Многострочный комментарий
<!--
Это пример многострочного
комментария
-->

key : "value"
numbers : { 1. 2. 3. 4 }
settings : ([
  timeout : 300,
  enabled : true
])

const_val := !(age)